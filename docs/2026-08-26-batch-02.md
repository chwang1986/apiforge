# 开发日志 — 2026-08-26 Batch 02

## 概述

全项目文件审查与优化，修复代码一致性问题，补全测试，更新文档。

## 修复内容

| 文件 | 变更 | 原因 |
|------|------|------|
| `src/server.py` | 移除底部空 `app` 实例；`run()` 中 reload=True 时抛出明确错误提示 | 避免加载错误实例；reload 与对象传入不兼容 |
| `src/router.py` | 重写 `register_tool`，改用 Pydantic 动态模型（与 server.py 一致） | 原实现用 `**kwargs` 会导致 422 |
| `src/router.py` | 统一使用 `get_type_hints` + `create_model` 方式 | 保持框架内部一致 |
| `src/middleware/__init__.py` | 添加 docstring 说明规划方向 | 避免空文件 |
| `pyproject.toml` | 移除未使用的 `pytest-asyncio`；添加 `httpx` 依赖 | 清理无用依赖；TestClient 需要 httpx |
| `pyproject.toml` | 移除 `asyncio_mode` 配置 | 不再需要 |
| `tests/test_basic.py` | 新增 reverse / 负数 / 422 / OpenAPI description 共 4 个测试 | 提升覆盖率 |
| `README.md` | 补充 docs/ 目录；修正快速开始命令；添加核心用法示例 | 文档完整性 |
| `docs/user-guide.md` | 新增"方式三：自定义服务"；修正常见问题表；更新测试预期输出 | 更完整的使用指引 |

## 当前状态

- [x] 项目骨架
- [x] 核心 Server 类
- [x] 路由工具（与核心一致）
- [x] 基础测试（8 个用例）
- [x] 用户手册
- [ ] 中间件（日志 / 限流 / 鉴权）
- [ ] 配置系统
- [ ] 发布打包
